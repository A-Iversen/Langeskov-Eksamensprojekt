<UserControl x:Class="MVVM.View.UserControls.RunnerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="570" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        
        <!--Left Panel-->
        <Border Grid.Column="0" Grid.RowSpan="2"
                CornerRadius="0,20,20,20" d:Background="{StaticResource bg}">

            <StackPanel Margin="5,20,0,0"
                        HorizontalAlignment="Left">

                <StackPanel Orientation="Horizontal">
                    <TextBox Style="{StaticResource textBox}" Tag="Fornavn"
                         Text="{Binding FirstName}"
                         Margin="0,20,0,0" Width="90"
                         BorderThickness="1.3,1.8,1.3,1"/>

                    <TextBox Style="{StaticResource textBox}" Tag="Efternavn"
                         Text="{Binding Lastname}"
                         Margin="5,20,0,0" Width="90"
                         BorderThickness="1.3,1.8,1.3,1"/>
                    
                </StackPanel>

                <ComboBox Style="{StaticResource ComboBoxStyle1}" Tag="køn"
                          Margin="0,20,0,0" Width="auto"
                          >

                </ComboBox>

                <StackPanel Orientation="Horizontal">
                    <TextBox Style="{StaticResource textBox}" Tag="Adresse"
                         Text="{Binding Adress}"
                         Margin="0,20,0,0" Width="120"/>

                    <TextBox Style="{StaticResource textBox}" Tag="Post Nr."
                         Text="{Binding PostalCode}"
                         Margin="5,20,0,0" Width="60"/>
                </StackPanel>

                <TextBox Style="{StaticResource textBox}" Tag="Email"
                         Text="{Binding Placeholder}"
                         Margin="0,20,0,0"
                         HorizontalAlignment="Left"
                         Width="185"/>

                <TextBox Style="{StaticResource textBox}" Tag="Telefon"
                         Text="{Binding Placeholder}"
                         Margin="0,20,0,0"
                         HorizontalAlignment="Left"
                         Width="185"/>

                <Calendar Style="{StaticResource CalendarStyle1}"
                          CalendarButtonStyle="{StaticResource CalendarButtonStyle1}" 
                          CalendarDayButtonStyle="{StaticResource CalendarDayButtonStyle1}" 
                          CalendarItemStyle="{StaticResource CalendarItemStyle1}"
                          Margin="0,20,0,0"
                          HorizontalAlignment="Left"
                          BorderThickness="1,1,1,2.3"
                          />




                <Button Style="{DynamicResource confirmationButton}" Height="35"
                        Width="160"
                        Margin="0,20,0,0"
                        Content="Tilføj Bruger">
                    
                </Button>

            </StackPanel>



        </Border>


        <!--Middle Buttons-->
        <RadioButton Style="{StaticResource addButton}"
                     Grid.Column="1" Grid.Row="1"
                     VerticalAlignment="Top"
                     HorizontalAlignment="Left"
                     Margin="0,0,0,0"
                     Panel.ZIndex="1">
            
            <Viewbox Width="20" Height="17" VerticalAlignment="Top"
                     Margin="0,5,3,0">
                <Path Style="{StaticResource AddUserLogo}"
                      Fill="White"/>
            </Viewbox>
        </RadioButton>

        <Button Style="{StaticResource removeButton}"
                Grid.Column="1" Grid.Row="1"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Left"
                Margin="5,0,0,15"
                IsEnabled="{Binding SelectedItem, ElementName=runnerDataGrid}">

            <Viewbox Width="20" Height="17" VerticalAlignment="Top"
                     Margin="-5,5,0,0">
                <Path Style="{StaticResource RemoveUserLogo}"
                      Fill="White"/>
            </Viewbox>
        </Button>
            
        
        <!--datagrid-->
        <Border Grid.Column="2" Grid.Row="1"
                CornerRadius="0,0,20,0" d:Background="{StaticResource bgDark}" Grid.ColumnSpan="2">

            <DataGrid x:Name="runnerDataGrid" RowStyle="{StaticResource DataGridRowStyle1}" 
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle1}" 
                      Style="{StaticResource DataGridStyle1}" 
                      CanUserAddRows="True"
                      HorizontalAlignment="Left"
                      Width="760" 
                        
                      Margin="3,15,0,0" ItemsSource="{Binding Runners}"
                      AutoGenerateColumns="False"
                      CanUserResizeColumns="False">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding RunnerID}"
                                        MinWidth="55"
                                        IsReadOnly="True"/>

                    <DataGridTextColumn Header="Navn" Binding="{Binding Name}"
                                        MinWidth="100"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>

                    <DataGridTextColumn Header="Køn" Binding="{Binding Gender}"
                                        MinWidth="65"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>

                    <DataGridTextColumn Header="Addresse" Binding="{Binding Address}" 
                                        MinWidth="95"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>

                    <DataGridTextColumn Header="Post Nr." Binding="{Binding PostalCode}" 
                                        MinWidth="85"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>

                    <DataGridTextColumn Header="Telefon" Binding="{Binding PhoneNumber}" 
                                        MinWidth="85"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>

                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" 
                                        MinWidth="120"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>

                    <DataGridTextColumn Header="Fødselsdato" Binding="{Binding DateOfBirth, StringFormat='{}{0:d}'}" 
                                        Width="1"
                                        
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>

                    <DataGridComboBoxColumn Header="Subsidy" SelectedValueBinding="{Binding SubsidyGroupName}" 
                                            Width="80" />
                    

                    
                </DataGrid.Columns>
            </DataGrid>

        </Border>
    </Grid>
</UserControl>
