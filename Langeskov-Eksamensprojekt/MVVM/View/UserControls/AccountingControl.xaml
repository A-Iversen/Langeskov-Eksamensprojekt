<UserControl x:Class="MVVM.View.UserControls.AccountingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="570" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Column="0" Grid.RowSpan="2"
                CornerRadius="0,20,20,20"
                d:Background="{StaticResource bg}">

        </Border>

        <Border Grid.Column="1" Grid.Row="1"
                CornerRadius="0,0,20,0"
                d:Background="{StaticResource bgDark}">
            <DataGrid ItemsSource="{Binding MemberGroups}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Group ID" Binding="{Binding MemberGroupID}" />
                    <DataGridTextColumn Header="Group Name" Binding="{Binding MemberGroupName}" />
                    <DataGridTextColumn Header="Subscription Fee" Binding="{Binding SubscriptionFee}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
