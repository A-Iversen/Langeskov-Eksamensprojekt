<UserControl x:Class="MVVM.View.UserControls.AccountingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="570" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Column="0" Grid.RowSpan="2"
                CornerRadius="0,20,20,20"
                d:Background="{StaticResource bg}">
            <StackPanel Orientation="Vertical" Margin="0,50,0,0">
                <Button Content="Runner Groups" Command="{Binding ShowRunnerGroupsCommand}" Style="{StaticResource viewSwitcherButton}"/>
                <Button Content="Tilskudsoversigt" Command="{Binding ShowSubsidySummaryCommand}" Style="{StaticResource viewSwitcherButton}"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Grid.Row="1"
                CornerRadius="0,0,20,0"
                d:Background="{StaticResource bgDark}">
            <Grid>
                <!-- DataGrid to display a list of runner groups -->
                <DataGrid ItemsSource="{Binding RunnerGroups}" AutoGenerateColumns="False" Width="300"
                          Visibility="{Binding RunnerGroupsVisibility}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Gruppe ID" Binding="{Binding MemberGroupID}" />
                        <DataGridTextColumn Header="Gruppenavn" Binding="{Binding MemberGroupName}" />
                        <DataGridTextColumn Header="Kontingent" Binding="{Binding SubscriptionFee}" />
                    </DataGrid.Columns>
                </DataGrid>
                
                <!-- ListView to display the summary of runners per subsidy group -->
                <ListView ItemsSource="{Binding SubsidyGroupSummary}" Margin="10"
                          Visibility="{Binding SubsidySummaryVisibility}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Tilskuds Gruppe" DisplayMemberBinding="{Binding SubsidyGroupName}" Width="200"/>
                            <GridViewColumn Header="Antal Løbere" DisplayMemberBinding="{Binding RunnerCount}" Width="150"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
