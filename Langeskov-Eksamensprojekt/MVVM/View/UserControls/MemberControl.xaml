<UserControl x:Class="MVVM.View.UserControls.MemberControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="570" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Column="0" Grid.RowSpan="2"
                CornerRadius="0,20,20,20" d:Background="{StaticResource bg}">

        </Border>

        <Border Grid.Column="1" Grid.Row="1"
                CornerRadius="0,0,20,0" d:Background="{StaticResource bgDark}">
            
            <DataGrid RowStyle="{DynamicResource DataGridRowStyle1}" 
                      ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                      Style="{DynamicResource DataGridStyle1}" 
                      CanUserAddRows="True"
                      HorizontalAlignment="Left"
                      Width="auto"
                        
                      Margin="15,15,0,0" ItemsSource="{Binding Members}"
                      AutoGenerateColumns="False"
                      CanUserResizeColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding MemberID}"
                                        MinWidth="55"
                                        IsReadOnly="True"/>
                   
                    <DataGridTextColumn Header="Navn" Binding="{Binding Name}"
                                        MinWidth="100"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>                    
                   
                    <DataGridTextColumn Header="Køn" Binding="{Binding Gender}"
                                        MinWidth="65"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>
                  
                    <DataGridTextColumn Header="Addresse" Binding="{Binding Address}" 
                                        MinWidth="95"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>
                  
                    <DataGridTextColumn Header="Post Nr." Binding="{Binding PostalCode}" 
                                        MinWidth="85"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>
                    
                    <DataGridTextColumn Header="Telefon" Binding="{Binding PhoneNumber}" 
                                        MinWidth="85"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>
                   
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" 
                                        MinWidth="120"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>
                   
                    <DataGridTextColumn Header="Fødselsdato" Binding="{Binding DateOfBirth}" 
                                        Width="110"
                                        EditingElementStyle="{StaticResource CellEditingStyle1}"/>
                </DataGrid.Columns>
            </DataGrid>
            
        </Border>
    </Grid>
</UserControl>
