<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style x:Key="textBox" TargetType="TextBox">
        <Setter Property="Height" Value="33"/>
        <Setter Property="Width" Value="150"/>
        <Setter Property="BorderBrush" Value="{StaticResource textMuted}"/>
        <Setter Property="BorderThickness" Value="1"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                        </Grid.ColumnDefinitions>

                        <Border Background="{StaticResource bgDark}" Grid.ColumnSpan="2" CornerRadius="15"/>

                        <Label x:Name="tbLabel" Content="{Binding Path=Tag, RelativeSource={RelativeSource AncestorType=TextBox}}"
                               VerticalAlignment="Center" Margin="5,0,0,0"  Visibility="Collapsed"
                               Foreground="{StaticResource textMuted}"
                               FontStyle="Italic" FontSize="{TemplateBinding FontSize}"/>
                        
                        <Border Background="Transparent" Grid.ColumnSpan="2"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="15">

                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="5,0,5,0"
                                          VerticalAlignment="Center"
                                          VerticalScrollBarVisibility="Disabled"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="tbLabel" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource info}"/>
                        </Trigger>
                        <Trigger Property="IsSelectionActive" Value="True">
                        </Trigger>
                    </ControlTemplate.Triggers>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>